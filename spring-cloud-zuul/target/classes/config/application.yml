server:
  port: 8885
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
spring:
  application:
    name: client-zuul
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: client-ribbon
    api-b:
      path: /api-b/**
      serviceId: client-feign